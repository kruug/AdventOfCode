# First number is minimum count
# Second number is maximum count
# Third element is the letter
import re

def isPasswordValid(min, max, let, password):
  
  return something

minCount = 0
maxCount = 0
letter = 'a'
password = 'password'
validCount = 0

inputPasswords = []

with open('input.txt') as inputFile:
  inputLines = inputFile.readlines()

for inputs in inputLines:
  inputPasswords.append(re.split('- :\n',inputs))

for passwords in inputPasswords:
  if isPasswordValid(passwords[0], passwords[1], passwords[2], passwords3):
    validCount++